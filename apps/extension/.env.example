# Extension (public config only)

# Community builds use BYOK: users paste Gemini/OpenAI keys in Settings.
# No TinAiLens analyze server required.

# Optional: only if you still run the NestJS API for local experiments
# PLASMO_PUBLIC_API_URL=http://localhost:3001
